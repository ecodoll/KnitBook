import LoadingState from "@/components/knitbook/shared/LoadingState";

/**
 * 작품 상세 진입 시 골격 화면을 보여 준다.
 */
const ProjectDetailLoading = () => {
  return <LoadingState variant="detail" />;
};

export default ProjectDetailLoading;
