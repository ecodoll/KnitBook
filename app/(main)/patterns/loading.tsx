import PatternsPageFallback from "@/components/knitbook/patterns/PatternsPageFallback";

/**
 * 도안 목록 진입 시 카드 골격을 바로 보여 준다.
 */
const PatternsLoading = () => {
  return <PatternsPageFallback />;
};

export default PatternsLoading;
